{
  "name": "daybreak",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.1",
    "hono": "^4.8.5"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "wrangler": "^4.110.0"
  }
}
